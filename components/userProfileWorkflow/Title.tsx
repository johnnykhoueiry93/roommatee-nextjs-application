import '../../styles/userProfileWorkflow/Title.css'

//@ts-ignore
const Title = ({ text }) => {
  return (
    <div className='component-title-format'>
      <h2>{text}</h2>
    </div>
  );
};

export default Title;
